import React from 'react';

// This is a named export because it includes export before the const keyword
// With named imports, you can export multiple values
export const NamedExports = () => <h1>Hello World</h1>;
export const AnotherNamedExport = () => <p>Hello from a named export</p>
